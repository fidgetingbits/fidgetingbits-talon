# basically I want to create a callback that's only enabled when I've set the
# VPN tunnel to start, and as soon as it detects that the nm_applet is running,
# then it automatically inserts the pin following roughly how the net man two
# factor command currently works
